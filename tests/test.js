const assert = require('assert');

describe('auto-repo-mowiauto-1', function() {
  it('should run without errors', function() {
    const main = require('../index.js');
    assert.ok(main);
  });
});
